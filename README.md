# Sample application with Cypress, NPM & Typescript

This simple module shows how to setup Cypress with NPM + Typescript. 

In order to try it out yourself, `git clone` this repository and call `npm install`


